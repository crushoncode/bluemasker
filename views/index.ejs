<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=
    , initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-WskhaSGFgHYWDcbwN70/dfYBj47jz9qbsMId/iRN3ewGhXQFZCSftd1LZCfmhktB" crossorigin="anonymous">
    <title>My App</title>
</head>
<body>
    <h1 class="alert alert-primary" align="center">BlueMasker</h1>
    
    Add a mask:
    <form action="/masks" method="POST">
        <select name="model">
            <option value="Level 1">Level 1</option>
            <option value="Level 2">Level 2</option>
        </select>
        <select name="size">
            <option value="Kid">Kid</option>
            <option value="Adult">Adult</option>
        </select>
        <select name="color">
            <option value="black">Black</option>
            <option value="white">White</option>
        </select>
        <select name="logo">
            <option value="heart">Heart</option>
            <option value="bear">Bear</option>
            <option value="mustache">Mustache</option>
        </select>
        <button type="submit">Submit</button>
    </form>
    <br />
    
    <div class="d-flex flex-wrap justify-content-around">
    <% for(var i=0; i<masks.length; i++) {%>
            <div class="card" style="width: 18rem;">
                    <img class="card-img-top" src=<%= masks[i].img %> alt="Card image cap">
                    <div class="card-body">
                      <h4 class="card-title"><%= `${masks[i].model} ${masks[i].size} ${masks[i].logo}` %></h4>
                      <h5 class="card-subtitle mb-2 text-muted">Model: <%= masks[i].model %></h5>
                      <h5 class="card-subtitle mb-2 text-muted">Size: <%= masks[i].size %></h5>
                      <h5 class="card-subtitle mb-2 text-muted">Color: <%= masks[i].color %></h5>
                      <h5 class="card-subtitle mb-2 text-muted">Logo: <%= masks[i].logo %></h5>
                      <h5 class="card-subtitle mb-2 text-muted">Price: $<%= (masks[i].price / 100) %></h5>
                      <a href="#" class="btn btn-danger">Delete</a>
                    </div>
            </div>
            <br />
    <% } %>
    </div>



</body>
</html>